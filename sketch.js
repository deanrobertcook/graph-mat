import {mat} from './mat.js'
import {graph} from './graph.js'

window.mat = mat;
window.graph = graph;


window.setup = function() {
  createCanvas(500, 500);
  background(51);

  // ans.vectors.forEach(v => {
  //   point(v[0], v[1]);
  // });
}

window.draw = function() {
  
}

